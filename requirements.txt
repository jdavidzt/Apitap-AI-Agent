# Apitap AI Agent - Python Dependencies
# For Mac Mini M4 (Apple Silicon) local setup
# No strict version pins — let pip find pre-built wheels for ARM64

# PyTorch - installed first via setup-mac.sh
torch
torchaudio

# Core Dependencies
fastapi
uvicorn[standard]
python-multipart

# AI/ML Models (whisper and TTS installed separately in setup-mac.sh)
requests

# Audio Processing
numpy

# Database
mysql-connector-python

# Utilities
python-dotenv
pydantic
